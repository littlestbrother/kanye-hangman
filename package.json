{
  "name": "kanye-hangman",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "yarn build && aws s3 sync --acl=public-read dist/ s3://www.ww-yeezus-do.net --profile aaron"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^1.1.0",
    "svelte": "^3.52.0",
    "vite": "^3.2.3"
  },
  "dependencies": {
    "axios": "^1.2.1",
    "bad-words": "^3.0.4",
    "svelte-toggle": "^3.1.0"
  }
}
